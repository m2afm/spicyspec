# notify

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build notify` to build the library.

## Running unit tests

Run `nx test notify` to execute the unit tests via [Vitest](https://vitest.dev/).
