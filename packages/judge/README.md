# judge

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build judge` to build the library.

## Running unit tests

Run `nx test judge` to execute the unit tests via [Vitest](https://vitest.dev/).
