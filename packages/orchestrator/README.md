# orchestrator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build orchestrator` to build the library.

## Running unit tests

Run `nx test orchestrator` to execute the unit tests via [Vitest](https://vitest.dev/).
